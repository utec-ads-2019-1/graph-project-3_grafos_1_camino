# Creen su rama y commiteen en ella :)


Crear rama
```bash
git checkout -b Daniel
git add --all
git commit -m 'Avance'
git push origin Daniel
```

Actualizar con master
```bash
git pull
git merge master
```

Correr el programa
```bash
make
./run
```

